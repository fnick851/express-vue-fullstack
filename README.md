# Minimalistic Express + Vue fullstack boilerplate

```bash
# development
npm run dev
# clean build folder
npm run clean
# build SPA only
npm run build-ui
# deployment
npm run start
```

## Features

- Hot reloading with `nodemon` and `parcel`
- Logging with `morgan`
